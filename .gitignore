# Parallel-agent worktrees checked out inside the repo root
quran-studio-quiz-fix/

# Audacity project files: the editing sources for the mp3s. Hundreds of MB
# each and not LFS-tracked, so they stay local — the exported *.mp3 in LFS
# are what this repo ships.
*.aup3
*.aup3-wal
*.aup3-shm
